<% include ../partials/header.ejs %>

<div class="ui grid">
    <div class="ui main text container segment">
        <div class="ui huge header">Add comment to <%= blog.title%></div>
        <form class="ui form" action="/blogs/<%= blog._id %>/comments" method="post">
            <div class="field">
                <label>Text</label>
                <input type="text" name="comment[text]" placeholder="text" required minlength="1" maxlength="256">
            </div>
            <button class="ui fluid black button">Submit</button>
        </form>
    </div>
</div>

<% include ../partials/footer.ejs %>